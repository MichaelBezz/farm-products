import styled from 'styled-components';

export const Option = styled.li`
  margin-bottom: 8px;
  font-size: 14px;
  line-height: 1.5;

  &:last-child {
    margin-bottom: 0;
  }
`;

export const Property = styled.span`
  font-weight: bold;
`;
